<%- include("../../views/partials/user/header") %>
<br><br><br><br>

<!-- Breadcrumb -->
<nav>
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page">Wishlist</li>
    </ol>
</nav>

<div class="container">
    <h2 class="text-center">Your Wishlist</h2>
    <div class="row">
        <% if (wishlist.length > 0) { %>
            <% wishlist.forEach(item => { %>
                <div class="col-md-4 mb-4">
                    <div class="product-card">
                        <img src="/uploads/product-images/<%= item.imageUrl %>" alt="<%= item.name %>" class="img-fluid">
                        <h3><%= item.name %></h3>
                        <p>Price: ₹<%= item.price %></p>
                        <div>
                            <form action="/add-to-cart-from-wishlist" method="POST" class="d-inline">
                                <input type="hidden" name="productId" value="<%= item.productId %>">
                                <button type="submit" class="btn btn-primary">Add to Cart</button>
                            </form>
                            
                            <form action="/remove-from-wishlist" method="POST" class="d-inline">
                                <input type="hidden" name="productId" value="<%= item.productId._id %>">
                                <button type="submit" class="btn btn-danger">Remove</button>
                            </form>
                            
                        </div>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <p class="text-center">Your wishlist is empty.</p>
        <% } %>
    </div>
</div>

<%- include("../../views/partials/user/footer") %>

<style>
    .breadcrumb {
        background-color: #ffffff;
        padding: 10px 50px;
        border-radius: 5px;
        margin-bottom: 0px;
    }
    .breadcrumb a {
        text-decoration: none;
        color: #007bff;
    }
    .breadcrumb a:hover {
        text-decoration: underline;
    }
    .product-card {
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 8px;
        margin: 10px;
        text-align: center;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        background-color: #fff;
    }
    .product-card img {
        width: 100%;
        height: auto;
        border-radius: 5px;
    }
    .product-card h3 {
        font-size: 1.2rem;
        margin: 10px 0;
    }
    .product-card p {
        font-size: 1rem;
        color: #555;
    }
    .product-card button {
        margin: 5px;
    }
</style>
